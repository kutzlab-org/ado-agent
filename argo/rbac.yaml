# ServiceAccount used by workflow pods and the Argo Events sensor.
# Both need permissions to submit Workflows and update Deployments.
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ado-agent-workflow-sa
  namespace: argo-events
---
# Allows the workflow to patch Deployments (for the deploy steps)
# and allows the sensor to submit Workflows.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ado-agent-workflow-role
  namespace: argo-events
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["workflows", "workflowtemplates"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "patch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ado-agent-workflow-rolebinding
  namespace: argo-events
subjects:
  - kind: ServiceAccount
    name: ado-agent-workflow-sa
    namespace: argo-events
roleRef:
  kind: Role
  apiGroup: rbac.authorization.k8s.io
  name: ado-agent-workflow-role
---
# Allow sensor SA to create workflows in argowf (where the WorkflowTemplate lives)
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ado-agent-workflow-role
  namespace: argowf
rules:
  - apiGroups: ["argoproj.io"]
    resources: ["workflows", "workflowtemplates"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ado-agent-workflow-rolebinding
  namespace: argowf
subjects:
  - kind: ServiceAccount
    name: ado-agent-workflow-sa
    namespace: argo-events
roleRef:
  kind: Role
  apiGroup: rbac.authorization.k8s.io
  name: ado-agent-workflow-role
---
# Allow workflow pods to patch Deployments in ado-agent namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ado-agent-deploy-role
  namespace: ado-agent
rules:
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "patch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ado-agent-deploy-rolebinding
  namespace: ado-agent
subjects:
  - kind: ServiceAccount
    name: ado-agent-workflow-sa
    namespace: argo-events
roleRef:
  kind: Role
  apiGroup: rbac.authorization.k8s.io
  name: ado-agent-deploy-role

---
