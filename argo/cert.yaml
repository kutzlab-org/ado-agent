apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-events-tls
  namespace: argo-events
spec:
  secretName: argo-events-tls
  commonName: argo-events.rss.kutzlab.io
  dnsNames:
    - argo-events.rss.kutzlab.io
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer